machine:
  ruby:
    version: 2.4.0
compile:
  override:
    - make build
deployment:
  rubygems:
    branch: master
    commands:
      - curl --data-binary @lmhd-${VERSION}.gem -H "Authorization:${RUBYGEMS_API_KEY}" https://rubygems.org/api/v1/gems
